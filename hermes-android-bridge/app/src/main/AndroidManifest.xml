<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- HTTP server -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Wake screen for background actions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Overlay HUD -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Foreground service to keep bridge alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- Screenshots via MediaProjection (Phase 2 enhancement) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Query all installed apps (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.telephony.gsm" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <!-- Set timers and alarms via Clock app (SET_TIMER intent) -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <!-- Run commands inside Termux (host terminal) — requires allow-external-apps=true in Termux -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- Package visibility (Android 11+) for Termux/Shizuku. QUERY_ALL_PACKAGES above already
         covers this, but declare explicit queries as a fallback for stricter ROMs. -->
    <queries>
        <package android:name="com.termux" />
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:name=".BridgeApplication"
        android:label="Hermes Bridge"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.HermesBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver for intent-driven relay control -->
        <receiver
            android:name=".service.RelayIntentReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.hermesandroid.bridge.START" />
                <action android:name="com.hermesandroid.bridge.STOP" />
                <action android:name="com.hermesandroid.bridge.STATUS" />
                <action android:name="com.hermesandroid.bridge.ENABLE_A11Y" />
                <action android:name="com.hermesandroid.bridge.ENABLE_SCREEN_RECORD" />
                <action android:name="com.hermesandroid.bridge.START_SCREEN_RECORD" />
                <action android:name="com.hermesandroid.bridge.STOP_SCREEN_RECORD" />
            </intent-filter>
        </receiver>

        <!-- Tasker v2.1.x result receiver — retained for diagnostic/monitoring only.
             All result submission goes through HTTP loopback to TaskerResultHttpHandler.
             The receiver is NOT exported so no external app can reach it. -->
        <receiver
            android:name=".tasker.TaskerResultReceiver"
            android:exported="false" />

        <!-- Foreground service for relay connection lifecycle -->
        <service
            android:name=".service.RelayService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Accessibility service declaration -->
        <service
            android:name=".service.BridgeAccessibilityService"
            android:exported="true"
            android:foregroundServiceType="specialUse|mediaProjection"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification listener service -->
        <service
            android:name=".service.BridgeNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Shizuku provider — exposes the binder so the app can run privileged shell commands -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Tasker result ContentProvider — accepts result envelopes via Binder IPC,
             bypassing Android 14 localhost network restrictions. Tasker JavaScriptlet
             uses context.getContentResolver().insert(...) to submit results. -->
        <provider
            android:name=".server.TaskerResultContentProvider"
            android:authorities="${applicationId}.taskerresult"
            android:exported="true"
            android:enabled="true" />

    </application>
</manifest>
